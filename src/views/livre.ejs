<!-- <table>
    <thead>
      <tr>
        <td>n°</td>
        <td>Nom</td>
        <td>Actions</td>
      </tr>
    </thead>
    <tbody>
      <% if (data) { %>
      <% for(var i = 0; i < data.length; i++) { %>
      <tr>
        <td><%= (i + 1) %></td>
        <td><%= data[i].titre %></td>
        <td><%= data[i].date_parution %></td>
         <td>
          <a href="role/update/<%= data[i].id %>" class="btn btn-info">Modifier</a>
          <a href="role/delete/<%= data[i].id %>" class="btn btn-danger">Supprimer</a>
        </td> 
      </tr>
      <% } %>
      <% } %>
    </tbody>
  </table> -->
  <p>Je suis la </p>